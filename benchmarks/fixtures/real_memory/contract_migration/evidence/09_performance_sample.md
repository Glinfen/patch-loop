# Performance sample

Synthetic traffic measurements covered warmup, steady state, and cooldown. Median latency varied within the expected range. The sample used placeholder identifiers and measured allocation counts, garbage collection pauses, response size, and socket reuse. It proposes no source change and contains no normative API language.

Retain this only as non-authoritative operational context.
